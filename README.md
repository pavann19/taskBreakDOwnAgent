# Documentation for `taskBreakDOwnAgent`

---

## File: `task_breakdown_ui_gemini.py`

### `def stream_gemini_reply(prompt)`
> The function `stream_gemini_reply` streams and yields the Gemini AI model's response to a given prompt, accumulating and returning text chunks as they arrive.

### `def build_task_breakdown_prompt(task)`
> The function generates a prompt instructing an agent to decompose a given task into detailed subtasks, specifying their purpose, importance, and resource requirements.
